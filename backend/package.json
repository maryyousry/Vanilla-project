{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
"scripts": {
  "server": "json-server --watch db.json --port 4000",
  "start": "node server.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
